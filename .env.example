# Copy these public values to frontend/.env.local for local development.
# They are intentionally Vite-prefixed because they are safe to ship to the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
